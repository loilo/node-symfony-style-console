/**
 * This is a dummy interface.
 * 
 * It will probably be needed as soon as we implement the Question API.
 */
interface InputInterface {
  isInteractive (): boolean
}

export default InputInterface
