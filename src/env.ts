/**
 * Export some machine-related information.
 */

/**
 * The character that separates segments of a path in the running operating system.
 */
export const DIRECTORY_SEPARATOR: string = require('path').sep

/**
 * The character(s) that indicate the end of a line in the running operating system.
 */
export const EOL: string = require('os').EOL
